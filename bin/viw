#!/usr/bin/env zsh

# viw = "Vim which"
# Finds a script file, shell alias, or shell function and edits it.
# If it's an alias or function, it opens Vim right to the line where it's
# defined.
#
# Usage:
#
#     $ viw myalias
#     $ viw git-merge-to

vim "$(find-location-of "$1")"
