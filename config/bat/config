# This is `bat`s configuration file. Each line either contains a comment or a
# command-line option that you want to pass to `bat` by default. You can run
# `bat --help` to get a list of all possible configuration options.

# Specify desired highlighting theme (e.g. "TwoDark").
# To preview all themes on a given file:
# bat --list-themes | fzf --preview="bat --theme={} --color=always /path/to/file"
--theme="TwoDark"

# Enable this to use italic text on the terminal.
--italic-text=always

# Syntax mappings: map a certain filename pattern to a language.
--map-syntax "vimrc:VimL"
--map-syntax "zshrc:Bourne Again Shell (bash)"
